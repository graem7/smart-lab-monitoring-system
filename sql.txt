CREATE TABLE ds18b20 (
    id INT AUTO_INCREMENT PRIMARY KEY,
    value FLOAT,
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE dht22_temperature (
    id INT AUTO_INCREMENT PRIMARY KEY,
    value FLOAT,
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE dht22_humidity (
    id INT AUTO_INCREMENT PRIMARY KEY,
    value FLOAT,
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE ldr1 (
    id INT AUTO_INCREMENT PRIMARY KEY,
    value FLOAT,
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE ldr2 (
    id INT AUTO_INCREMENT PRIMARY KEY,
    value FLOAT,
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE pir (
    id INT AUTO_INCREMENT PRIMARY KEY,
    value INT,
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);
